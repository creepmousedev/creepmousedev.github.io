<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hey! I'm Adam.</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>

    <div class="container">
        <header class="border-bottom lh-1 py-3">
          <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="col-4 pt-1">
              <!--<a class="link-secondary" href="#">Subscribe</a>-->
            <% if(!locals.temp && !locals.forecast){ %>
              <form action="/" method="post" id="positionForm">
                <input type="hidden" name="lat" id="lat">
                <input type="hidden" name="lon" id="lon">
              </form>
            <% } else{ %>
              <p id="temperature"><%= temp %>°F</p>
              <p id="forecast"><%= forecast %></p>
            <% } %>
            </div>
            <div class="col-4 text-center">
              <a class="blog-header-logo text-body-emphasis text-decoration-none" href="/">Hi, I'm Adam!</a>
            </div>
            <div class="col-4 d-flex justify-content-end align-items-center">
              <!--<a class="link-secondary" href="#" aria-label="Search">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="mx-3" role="img" viewBox="0 0 24 24"><title>Search</title><circle cx="10.5" cy="10.5" r="7.5"></circle><path d="M21 21l-5.2-5.2"></path></svg>
              </a>-->
              <form action="/newPost" method="post">
                <!--<button class="btn btn-sm btn-outline-secondary" href="#" id="newPostButton">Create New Post</button>-->
              </form>
            </div>
          </div>
        </header>
      
        <div class="nav-scroller py-1 mb-3 border-bottom">
          <nav class="nav nav-underline justify-content-between">
            <a class="nav-item nav-link link-body-emphasis <%=aboutActive%>" href="/about">About</a>
            <a class="nav-item nav-link link-body-emphasis <%=contactActive%>" href="/contact">Contact</a>
            <a class="nav-item nav-link link-body-emphasis <%=articlesActive%>" href="/articles">Pokemon</a>
            <a class="nav-item nav-link link-body-emphasis <%=projectActive%>" href="/projects">Projects</a>
          </nav>
        </div>

        <% if(locals.image){ %>
          <p style="text-align: center;">Click Pokemon below for next card.</p>
          <div id="cardContainer">
          <div id="card">
            <div id="pokemonOfTheDay" style="background-color: <%=color%>;">
              <h4 id="pokemonName"><%= name %></h4>
              
              <div id="evolveContainer">
                  <% if(locals.evolveImage) { %>
                    <img id="evolveFromImage" src= <%= evolveImage %> height="75">
                  <% } %>
                  <p id="evolveFrom"><%= evolvesFrom %></p>
              </div>
            
              <img id="pokemonImage" src= <%= image %> height="225">
              <h5 id="pokemonType"><%= type %> type</h5>
            </div>
            <div id="back"></div>
          </div>
          </div>
        <% } %>